import { describe, expect, it, vi } from 'vitest';
import { dbRunner } from './lib/util.js';

describe('Transaction Log', () => {
	it('should create a transaction log', () => dbRunner(async ({ db }) => {
		//
	}));
});